<%= form_for @review do |f| %>
    <%= f.hidden_field :product_id, value: @review.product.id %>
    <%= render partial: "layouts/errors", locals: {record: @review}  %>
    <%= f.label :content %>
    <%= f.text_area :content, cols: 30, rows: 10 %>
    <%= f.submit  %>
    <% end %>